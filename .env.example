ANTHROPIC_API_KEY=
PIXABAY_API_KEY=
PORT=3001
# Prompt caching: on (default) or off. Off = no cache write/read, system prompt
# billed as normal input every turn. Restart the server after changing.
PROMPT_CACHING=on
